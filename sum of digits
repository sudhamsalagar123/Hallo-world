#include<stdio.h>
int sum (int a);
int mrn 0;
}
int sum (int num)
{
	if(num!=0)
	{
	return(num %10+sum(num/10));
}
else
{
	return 0;ain()
{
	int num,result;
	printf("Enter the number :");
	scanf("%d",&num);
   result=sum(num);
   printf("Sum of dihts in %d is %d\n",num,result);
   retu
}
}





out put is 
Enter the number =1234
ans=10;
